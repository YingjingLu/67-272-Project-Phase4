<h1>Items Index Page</h1><br/>

<% if @boards.blank? && @pieces.blank? && @clocks.blank? && @supplies.blank? && @inactive_items.blank? %>
    <div>
        <p>There are no items in the system at this time </p>
    </div>

<% else %>
    <h2>Available Items</h2>
    <table>
        <tr>
            <th>Name</th>
            <th>Price</th>
            <th>Category</th>
            <th>Inventory</th>
        </tr>

        <% @boards.each do |board| %>
            <tr>
                <td><%= link_to board.name, item_path(board)%></td>
                <td><%= board.item_prices.current.chronological.first %></td>
                <td><%= board.category %></td>
                <td><%= board.inventory_level %></td>
            </tr>
        <% end %>

    </table>

    <table>
        <tr>
            <th>Name</th>
            <th>Price</th>
            <th>Category</th>
            <th>Inventory</th>
        </tr>

        <% @pieces.each do |piece| %>
            <tr>
                <td><%= link_to piece.name, item_path(piece)%></td>
                <td><%= piece.item_prices.current.chronological.first %></td>
                <td><%= piece.category %></td>
                <td><%= piece.inventory_level %></td>
            </tr>
        <% end %>

    </table>

    <table>
        <tr>
            <th>Name</th>
            <th>Price</th>
            <th>Category</th>
            <th>Inventory</th>
        </tr>

        <% @clocks.each do |clock| %>
            <tr>
                <td><%= link_to clock.name, item_path(clock)%></td>
                <td><%= clock.item_prices.current.chronological.first %></td>
                <td><%= clock.category %></td>
                <td><%= clock.inventory_level %></td>
            </tr>
        <% end %>

    </table>


    <table>
        <tr>
            <th>Name</th>
            <th>Price</th>
            <th>Category</th>
            <th>Inventory</th>
        </tr>

        <% @supplies.each do |supply| %>
            <tr>
                <td><%= link_to supply.name, item_path(supply)%></td>
                <td><%= supply.item_prices.current.chronological.first %></td>
                <td><%= supply.category %></td>
                <td><%= supply.inventory_level %></td>
            </tr>
        <% end %>

    </table>

    <table>
        <tr>
            <th>Name</th>
            <th>Price</th>
            <th>Category</th>
            <th>Inventory</th>
        </tr>

        <% @inactive_items.each do |inactive| %>
            <tr>
                <td><%= link_to inactive.name, item_path(inactive)%></td>
                <td><%= inactive.item_prices.current.chronological.first %></td>
                <td><%= inactive.category %></td>
                <td><%= inactive.inventory_level %></td>
            </tr>
        <% end %>

    </table>
<% end %>

<%= link_to 'New Item', new_item_path %>